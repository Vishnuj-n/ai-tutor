# Walkthrough: Database Dependency Injection & Secure Extension Loader

We have successfully refactored the Go backend to completely eliminate the global database connection variable, standardizing the dependency injection pattern around `*db.Repository` and securing the sqlite-vec extension loader.

## Key Changes

### 1. Database Layer Refactoring
- **Repository Injector**: Removed global `var conn *sql.DB` and `var embeddingDimension int32` in [store.go](file:///c:/Users/vishn/PROJECT/ai-tutor/internal/db/store.go) and replaced them with a struct-based `type Repository struct`.
- **Method Receiver Conversion**: All database package queries in `internal/db/` have been refactored into receiver methods on `*Repository`.
- **Wails App Wiring**: Injected `repo *db.Repository` into the Wails application and updated all endpoint calls in [app.go](file:///c:/Users/vishn/PROJECT/ai-tutor/app.go) and [notebook_endpoints.go](file:///c:/Users/vishn/PROJECT/ai-tutor/notebook_endpoints.go).
- **Service Layer Updates**: Updated `internal/study`, `internal/scheduler`, and `internal/retrieval` packages to accept the database repository dependency instead of making direct package-level calls to `db`.

### 2. Secure Extension Loader
- Modified dynamic library path resolution to validate that sqlite-vec extensions (`vec0.dll`, `vec0.dylib`, `vec0.so`) strictly reside within approved app cache, localappdata, or runtime asset directories, preventing directory traversal.

### 3. Test Coverage & Fixes
- Renamed the internal database test helper file `test_utils.go` to `test_utils_test.go` to fix compile errors in non-test builds.
- Refactored `app_contract_test.go` and internal DB tests to wire `testRepo` into mocked structures and the test App, successfully resolving all test suite compile errors and nil pointer panics.

## Verification
All tests are compile-clean and pass successfully. You can run:
```powershell
go test ./...
```
Output:
```
ok  	ai-tutor	5.396s
ok  	ai-tutor/internal/db	(cached)
ok  	ai-tutor/internal/embeddings	(cached)
ok  	ai-tutor/internal/llm	(cached)
ok  	ai-tutor/internal/notebook	(cached)
ok  	ai-tutor/internal/scheduler	(cached)
```
